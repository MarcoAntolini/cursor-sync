# Fetch and pull

Follow the project skill **git-fetch-pull** at `.cursor/skills/git-fetch-pull/SKILL.md`.

Run `git fetch --all`, then `git pull` for the current branch. Do not stage, commit, amend, rebase, or push unless I explicitly ask in this message.
