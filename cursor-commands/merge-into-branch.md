# Merge current branch into target

Follow the project skill **git-merge-into-branch** at `.cursor/skills/git-merge-into-branch/SKILL.md`.

Use the **target branch name** I give in this message (example: `main`, `staging`). Merge **the branch I was on when I ran this command** **into** that target: checkout target, merge the source branch, then **switch back** to the branch I started on. Do not push unless I explicitly ask in this message.

If I did not name a target branch, ask me once for it and stop until I answer.
