# Merge current branch into target

Follow the project skill **git-merge-into-branch** at `.cursor/skills/git-merge-into-branch/SKILL.md`.

Use the **target branch name** I give in this message (example: `main`, `staging`). Merge **the branch I was on when I ran this command** **into** that target: checkout target, merge the source branch, then **push the target branch** while still on target (`git push`, or `git push -u origin <target>` when no upstream exists and `origin` is available—if push fails or there is no usable remote, follow the skill: stay on target, report, and do not switch back until push succeeds or I explicitly say in this message to skip push and switch back). After a **successful** push, **switch back** to the branch I started on. Do **not** push my original branch or others unless I explicitly ask in this message.

If I did not name a target branch, ask me once for it and stop until I answer.
