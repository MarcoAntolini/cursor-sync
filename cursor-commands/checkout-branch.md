# Checkout branch

Follow the project skill **git-checkout-branch** at `.cursor/skills/git-checkout-branch/SKILL.md`.

Switch to the branch **named in this message**. If that branch does not exist locally or on `origin` after fetch, **ask me for explicit confirmation** before creating it; if I do not confirm, stop and do not create it.

If I did not name a branch, ask me once for the name and stop until I answer.
